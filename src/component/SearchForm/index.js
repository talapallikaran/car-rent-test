import SearchForm from './SearchForm';
/*
An index file is useful to populate the states and actions
when we are using any state management like Redux.

This file will also reduce the size of the main container/component
file because all the state and action mappings will occur here.
*/
export default SearchForm;
